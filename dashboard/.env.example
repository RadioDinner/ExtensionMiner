# Copy to .env.local for local `npm run dev`. On Vercel, set these as
# (server-side) Environment Variables instead.
#
# Service-role key is used server-side only (RLS is locked to service role).
# Never expose it to the browser / never prefix it with NEXT_PUBLIC_.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
