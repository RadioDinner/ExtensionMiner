1. Let's start session 7, get the handoff and kick off this next set of changes

2. commit to main for this session, merge everything you have to main and I'm going to delete the insprining whatever branch

3. A feature to the feature list. On the dashboard, the "rating vs installs" card, I want to be able to click the circles and have it load into the extension that the data circle represents, or the group of extensions that are represented in the data.

Add a second feature, I want the opportunity zone to be more than 25 extensions. I want it to be able to display all the ones in the zone adn then a filter to limit it to a certain amount, like 25.

4. I am curious, for the ranking layer, what does it do, in order that it does it? If I run the ranking layer, and then immediately run it again, does it burn more tokens? or does it check for what is different?

5. Also, I am still not seeing a displayed star or icon representing the extensions that have been deep dove

6. For the ranking layer, I want the re-run behavior to be more seemless, especially since we're adding in more than 25 opportunity extensions. But I want to modify my feature idea. Instead of doing more than 25, I want a way to sort and evaluate the running list via the dashboard. For example, The "Chrome Remote Desktop" extnesion is at the top of the opportunity list but it has 35 milion installs, and is published by google themselves. There is no way I compete with that, at least, not right now. So I want a dashboard or button on each row of the extensions in the opportunity zone where I can click, "Remove from zone" and add a cause. Make a few options for removal reason "Too large","Too complex","Uninterested" and "Publisher owned". Then I want the option to also bring them back to the pool, but whenever one is eliminated from the 25, I want more added to keep a list of 25

7. Let's make so the ranking layer checks for newly added extensions before running, and then also checks the queue for deep dive data. I want a button in the dashboard then to set the ranking layer to override and re-run across the board. I want that option to be a button toggle, so the ranking layer checks the setting saved in Supabase and then runs accordingly.
